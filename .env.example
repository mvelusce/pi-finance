# Environment variables for Pi Finance API

# API Keys (comma-separated list of valid API keys)
# Generate secure keys using: openssl rand -hex 32
API_KEYS=your-secret-api-key-here,another-key-if-needed

# CORS Origins (comma-separated list, or * for all)
# Example: https://sheets.google.com,https://script.google.com
CORS_ORIGINS=*

# Application Settings
APP_NAME=Pi Finance API
APP_VERSION=1.0.0
DEBUG=false

# API Server Port (default: 8080)
# Change this if port 8080 is already in use
API_PORT=8080
